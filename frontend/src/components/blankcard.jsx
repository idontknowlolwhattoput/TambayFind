export default function BlankCard({ width, height }) {
    return (
        <div
            className="flex rounded-xl overflow-hidden shadow-lg border border-gray-200"
            style={{ width, height }}
        >
        </div>
    );
}
